# koengide プロジェクト概要

戸田市の公園ガイドサイト（koengide.com）。GitHub連携・Vercel自動デプロイ。
姉妹リポジトリ koengide-pipeline（Private）で写真圧縮・HTML生成・GitHub自動コミットを行う
park-creator.js を管理している。認証情報は koengide-pipeline 側の `.credentials/.env` で管理。

## 環境・作業ルール

- 実行環境は Windows PowerShell。Linux系オプション（`ls -la`、`grep` 等）は使えないため、
  `Get-ChildItem -Force`、`Select-String` など PowerShell 相当のコマンドを使うこと。
- 作業は「方針・diff報告 → 確認 → 実行」の2段階フローを徹底する。
- 破壊的操作（git push / commit / reset / rebase、rm 等）は必ず事前に内容を報告し、
  確認を得てから実行する。

## データ・スキーマに関する注意

- 公園データは Supabase の `parks` テーブルで管理（既存87件。2026-08-16に二重登録の
  id43「上部公園」を id38「荒川水循環センター上部公園」に統合して88件から87件になった。
  `/parks/43.html` は削除し、`vercel.json` で `/parks/38.html` へ301リダイレクト）。
- `has_*` 系の設備フラグは bool 型だが NULL を含む三値管理（TRUE / FALSE / NULL）で扱う。
  一括の型変換・一括更新は行わない。
- 詳細ページの「✨この公園の特徴」3項目・「🌳公園について」3段落・写真の alt文は
  DB には存在せず、生成済みHTML内にのみ存在する。再生成する場合は、事前に
  既存HTMLからの抽出・保全を行うこと。

## コンテンツ生成ルール

- 推測記述の禁止を最優先ルールとする。survey_level が L2（現地訪問・写真あり）以外の場合、
  体感表現・評価表現・推量表現（例:「桜が綺麗」「静かな公園」等）は使用しない。
- 出典不明の評価（★評価等）を生成コンテンツに含めない。
- 未取得の項目は該当行ごと非表示にする（架空の情報で埋めない）。

## やってはいけないこと（禁止事項）

### 環境・作業場所関連

- 起動フォルダを間違えたまま作業しない。koengide関連の作業は必ず
  `claude-workspace_koengide` 配下で行う（過去にzukailabフォルダでの誤起動事故あり）。
- Web管理画面（Supabase等）での全文置換・一括置換を行わない
  （過去にファイル二重化事故の経験あり）。

### コンテンツ生成関連

- 推測・体感表現や出典不明の評価を生成コンテンツに含めない
  （上記「コンテンツ生成ルール」の推測記述禁止ルールを参照）。

### ファイル・データ関連

- 勝手にファイル・フォルダを削除しない（特に `rm -rf` 相当の再帰削除）。
- `.credentials/.env` や認証情報ファイルの中身を読み上げたり、ログに出力したりしない。
- `draft-parks.json`（未登録18公園の下書き）を本番のSupabase `parks` テーブルへ
  勝手に登録・反映しない（写真が揃うまで非公開が方針）。
- 既存87件のnotes等を、テンプレート再生成時に確認なしで一括上書き・削除しない
  （生成済みHTMLの特徴3項目・公園について3段落はDBに存在しないため、
  再生成前の抽出・保全が必須）。

### Git・デプロイ関連

- `git push` / `git commit` / `git reset` / `git rebase` を確認なしで実行しない
  （koengideはVercelと連携しており、pushが即本番反映につながる）。
- mainブランチへのforce pushは行わない。
- コミット前に必ず差分を確認する（過去に `parks/22_files` の誤コミット事故あり）。

### 依存関係関連

- `npm audit fix --force` を勝手に実行しない（sharpの脆弱性修正は破壊的変更を
  伴うため対応保留中）。
